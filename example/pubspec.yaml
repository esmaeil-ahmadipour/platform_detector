name: platform_detector_example
description: Demonstrates how to use the platform_detector plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  platform_detector:
    path: ../

flutter:
  uses-material-design: true
